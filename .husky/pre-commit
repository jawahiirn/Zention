#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running lint-staged..."
npx lint-staged || exit 1

echo ""
echo "🔍 Running knip to check for unused code (informational)..."
npm run knip || echo "⚠️  Knip found unused code. Please review when possible."

